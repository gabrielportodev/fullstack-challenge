{
  "name": "crash-game",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*",
    "frontend"
  ],
  "scripts": {
    "setup": "cp services/games/.env.example services/games/.env && cp services/wallets/.env.example services/wallets/.env && cp frontend/.env.example frontend/.env",
    "docker:up": "bun run setup && docker compose up",
    "docker:down": "docker compose down",
    "docker:prune": "docker compose down -v --rmi all --remove-orphans && docker volume prune -f",
    "format": "prettier --write .",
    "test": "bun test",
    "test:unit": "bun test unit",
    "test:e2e": "bun test e2e"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.8.3"
  }
}
